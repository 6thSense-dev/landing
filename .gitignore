# Large archives — keep local only
*.zip
*.7z
*.tar.gz

# Python
__pycache__/
*.pyc
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.log

# Catalog ingest scratch — regenerate with `make -C scripts/catalog`.
# Tens of MB of synthetic mp4/npz plus the built bundle; never committed.
scripts/catalog/sample/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Cursor / local agent & GSD planning (keep on disk; do not publish)
.cursor/
.planning/
tasks/

# Secrets
.env
.env.local
.env.*.local
!.env.example

# Node / frontend (Vite)
node_modules/
dist/
.vite/
frontend/dist/
frontend/node_modules/
landing/dist/
landing/node_modules/

# Superpowers design/plan docs — local only, not committed
docs/superpowers/

# Superpowers brainstorm visual companion session files — local only
.superpowers/
media-sources/
.gstack/

# Run artefacts from scripts/catalog/e2e/catalog_e2e.cjs. The screenshots beside
# them ARE committed evidence; these JSON dumps are per-run detail for triage.
docs/catalog/e2e-*.json

# Catalog: local dev runtime state and the generated sample corpus
scripts/catalog/.dev/

# Catalog: local S3 credentials for `dev_local.sh --s3` (never committed)
scripts/catalog/.env.local
